import React from "react";
import "./App.css";
import ModalExampleDimmer from "./Components/modal/Modal";

// I tried to do the whole coding part of this modal in a
// separate component so that this page is as empty as possible.
// In this section, I just import the components and use them.
// I use CSS in this project only to create flex.

const App = () => {
  return <ModalExampleDimmer />;
};

export default App;
